const Discord = require("discord.js");

module.exports = {
  name: "command_name",
  //params: [],
  info: "Info on command",
  //slash: false,
  category: "hidden",

  async execute(message, arguments, fsh) {
    
  }
};